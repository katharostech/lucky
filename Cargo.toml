[package]
name = "lucky"
version = "0.1.0"
authors = ["zicklag <zicklag@katharostech.com>"]
edition = "2018"

[dependencies]
clap = "2.33"
zip = { version = "0.5", features = ["bzip2"], default-features = false }

[build-dependencies]
zip = { version = "0.5", features = ["bzip2"], default-features = false }
walkdir = "2.2"